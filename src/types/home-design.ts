export interface HomeDesignProject {
  url: string;
  alt: string;
  title: string;
  subtitle: string;
  description: string;
}

export interface DesignFeature {
  title: string;
  description: string;
}

export interface ProcessStep {
  number: number;
  title: string;
  description: string;
}